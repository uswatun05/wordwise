import 'package:flutter/material.dart';

class TranslatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Terjemahan'),
      ),
      body: Center(
        child: Text('Halaman Translate'),
      ),
    );
  }
}
